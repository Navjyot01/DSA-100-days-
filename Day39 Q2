#include <stdlib.h>

int* topKFrequent(int* nums, int numsSize, int k, int* returnSize) {
    int* result = (int*)malloc(sizeof(int) * k);

    // Example dummy logic (replace with real logic)
    for (int i = 0; i < k; i++) {
        result[i] = nums[i];
    }

    *returnSize = k;
    return result; 
}
